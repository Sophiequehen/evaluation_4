#Evaluation 4

##Pour récupérer le repo : 
```
git clone git@github.com:Sophiequehen/evaluation_4.git
```

##Pour vous connecter à la base de données, aller dans le fichier connect.php et modifier les informations suivantes : 

```
$username = 'votreusername';
$password = 'votremdp';
```

##Créer une database du nom de 'database_eval4.sql' et Importer le fichier database_eval4.sql